# IN THE VOID — Android APK

هذه النسخة مبنية على نسخة الموقع النهائية التي تم إرسالها في 13 سبتمبر 2026.

## بناء APK على GitHub بدون Android Studio
1. ارفع محتويات هذا المجلد إلى Repository جديد على GitHub.
2. تأكد من وجود `.github/workflows/build-android.yml`.
3. افتح **Actions** → **Build IN THE VOID APK**.
4. اضغط **Run workflow**.
5. بعد نجاح البناء، افتح **Artifacts** وحمّل `IN_THE_VOID-APK`.
6. فك ملف الـZIP الناتج وستجد `app-debug.apk`.

## ملاحظات مهمة
- التطبيق يستخدم نفس Supabase الموجود في الموقع.
- الملفات التي يختار المستخدم تنزيلها من التطبيق تُحفظ داخل تخزين التطبيق (IndexedDB)، وليس في مجلد Downloads العام للهاتف.
- بعد حفظ الملف يتحول الزر تلقائيًا من **تحميل** إلى **فتح**، وحتى بعد الرجوع للصفحة يظل **فتح** طالما النسخة المحلية موجودة.
- PDF يعرض داخل التطبيق بواسطة PDF.js، ويوجد زر **فتح من برنامج آخر** لاختيار تطبيق PDF متوافق عبر شاشة المشاركة/الاختيار في Android.
- إدارة الأعضاء وإدارة الفيديوهات وفحص بصمات PDF صفحات محلية داخل التطبيق، وبها زر **رجوع** آمن إلى الصفحة السابقة أو التطبيق.
