# IN THE VOID — Android build

هذه النسخة هي تجهيز Android المبني على آخر نسخة للمشروع.

## ما تم إصلاحه
- شاشة البدء التفاعلية تعتمد على صفحة الفازة الأصلية قبل دخول المنصة.
- لا تظهر شاشة تسجيل الدخول للمستخدم الذي لديه Session محفوظة وصالحة.
- عند تأخر استعادة Session يتم الانتظار بصمت، وإذا فشل الاسترجاع يظهر `index3.html` كخطة احتياطية.
- زر رجوع Android يعالج النوافذ/الصفحات الداخلية قبل الخروج.
- تم تجهيز صفحات إدارة الأعضاء والفيديوهات وفحص PDF داخل التطبيق مع Back handling.
- الملفات المحفوظة داخل التطبيق تتحول أزرارها إلى «فتح» وتظل قابلة للفتح دون إعادة تنزيل.
- PDF يعرض داخل التطبيق مع زر «فتح من برنامج آخر».
- التطبيق يستطيع استخدام النسخة المحلية المحفوظة دون اتصال، ويعطي «لا يوجد اتصال بالإنترنت» عند طلب محتوى غير محفوظ.
- أيقونة Android تستخدم `android-icon.png`.
- تم ربط Firebase/FCM عبر `google-services.json` و`@capacitor/push-notifications`.
- Workflow ينفذ إعداد الأيقونة وملف Firebase قبل بناء APK.

## Firebase Push
التطبيق يسجل FCM token عندما يسمح المستخدم بالإشعارات. إرسال إشعارات الإدارة بشكل فوري من لوحة الإدارة يحتاج Backend sender في Supabase مع اعتماد Firebase الخاص بالسيرفر.

## GitHub
في GitHub افتح Actions ثم `Build IN THE VOID APK` ثم `Run workflow`.
