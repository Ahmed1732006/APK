# IN THE VOID — بناء APK

## ما تم تجهيزه
- التطبيق يستخدم ملفات الموقع داخل الـAPK عبر Capacitor، وليس فتح رابط خارجي.
- صفحات إدارة الأعضاء والفيديوهات وفحص البصمات مضمّنة داخل تجربة التطبيق.
- الفازة التفاعلية في `index.html` تبقى بوابة البداية في كل تشغيل.
- الجلسة محفوظة محليًا عبر Supabase؛ المستخدم الذي سبق له تسجيل الدخول لا يرى Login مرة أخرى ما دامت الجلسة صالحة.
- عند فشل/تأخر استعادة جلسة معروفة على Android، يتم تحويل المستخدم إلى `index3.html` المخصص بدل رسالة الخطأ العامة.
- زر الرجوع في Android يرجع خطوة واحدة داخل التطبيق، ويغلق النوافذ/العارض أولًا؛ وإذا لم توجد خطوة سابقة يسأل قبل الخروج.
- PDF يعمل داخل التطبيق باستخدام PDF.js محليًا، مع زر «فتح من برنامج آخر» باستخدام Android chooser عبر Capacitor Share.
- الملفات التي يختار المستخدم تحميلها تُحفظ داخل مساحة التطبيق الخاصة، ويصبح الزر «فتح» بدل «تحميل». الملفات المحفوظة تظل قابلة للفتح بدون إنترنت.
- الفيديوهات تُعرض داخل التطبيق ولا يظهر لها زر تحميل.
- عند عدم وجود اتصال، التطبيق يحاول استخدام آخر لقطة بيانات محفوظة؛ وإذا لم توجد بيانات محلية كافية تظهر صفحة `index3.html`.
- الإشعارات ورسائل الإدارة تُزامن محليًا ويجري عرض إشعار Android محلي عند اكتشاف عنصر جديد أثناء اتصال التطبيق/عودته للإنترنت.
- أيقونة Android تستخدم `android-icon.png`.

## بناء GitHub
Workflow: `.github/workflows/build-android.yml`

الخطوات: `npm install` → `npm run build:web` → `npx cap add android` → `node scripts/prepare-android.mjs` → `npx cap sync android` → `./gradlew assembleDebug`.

النتيجة: Artifact باسم `IN_THE_VOID-APK`.

> ملاحظة: إشعار Android الحقيقي أثناء كون التطبيق مغلقًا بالكامل يحتاج مزوّد Push مثل FCM/OneSignal مع إعداداته. النسخة الحالية تنفّذ مزامنة وإشعارات محلية عند تشغيل/عودة التطبيق للإنترنت بدون هذه الخدمة.
